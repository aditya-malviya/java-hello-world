# python-hello-world


Create a Virtual Environment with
python3 -m venv envname
Activate the Environment
Source envname/bin/activate
Install flask using pip
pip3 install Flask
It should install all the packages related to flask, then check if installed properly
flask --version
